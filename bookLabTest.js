function p1() {
    for (let i = 1; i < 14; i++){
        let text = document.getElementById(`giveText${i}`)
        if (i == 1) {
            text.innerHTML = `<p>Lybrate brings Pathological Labs to your phone.</p>
                    <p>Here are 4 major reasons you should book these tests through your Lybrate app.</p>
                    <p>1. Convenience: Your test samples (blood and urine) will be collected from right where you are.</p>
                    <p>2. Save Money: You save upto 30% on these tests.</p>
                    <p>3. Choose the Best: You get options of various labs to chose from and compare the offers, too.</p>
                    <p>4. Easy Access to Results: Your results are always saved on your Lybrate account, for an easy access and for posterity.</p>`
        } else {
            if (text.innerHTML.length != 0) {
                text.innerHTML=``
            }
        }
        
    }
}
function p2() {
    for (let i = 1; i < 14; i++){
        let text = document.getElementById(`giveText${i}`)
        if (i == 2) {
            text.innerHTML = `<p>You can book tests from your preferred Labs, like Thyrocare, Quest Diagnostics, Labstreet.</p>`
        }
        else {
            if (text.innerHTML.length != 0) {
                text.innerHTML=``
            }
        }
    }
}
function p3() {
    for (let i = 1; i < 14; i++){
        let text = document.getElementById(`giveText${i}`)
        if (i == 3) {
            text.innerHTML = `<p>Yes. Only NABL, CAP or equivalently certified labs qualify to become Lybrate Lab partners. Providing you with only the best Labs is our priority.</p>`
        } else {
            if (text.innerHTML.length != 0) {
                text.innerHTML=``
            }
        }
    }
}
function p4() {
    for (let i = 1; i < 14; i++){
        let text = document.getElementById(`giveText${i}`)
        if (i == 4) {
            text.innerHTML = `<p>Yes. Your Lab test booking will be initiated under your registered account, on Lybrate.</p>`
        } else {
            if (text.innerHTML.length != 0) {
                text.innerHTML=``
            }
        }
    }
}
function p5() {
    for (let i = 1; i < 14; i++){
        let text = document.getElementById(`giveText${i}`)
        if (i == 5) {
            text.innerHTML = `Home sample collection is completely safe and highly convenient. Trained staff visit you, equipped with all the necessary arrangement for collecting the samples. They are efficiently trained to take blood samples also and hygienically retrieve the samples to the respective Lab.`
        } else {
            if (text.innerHTML.length != 0) {
                text.innerHTML=``
            }
        }
        
    }
}
function p6() {
    for (let i = 1; i < 14; i++){
        let text = document.getElementById(`giveText${i}`)
        if (i == 6) {
            text.innerHTML = `<p>Yes. It is up to your convenience and it’s feasibility.</p>`
        }
        else {
            if (text.innerHTML.length != 0) {
                text.innerHTML=``
            }
        }
    }
}
function p7() {
    for (let i = 1; i < 14; i++){
        let text = document.getElementById(`giveText${i}`)
        if (i == 7) {
            text.innerHTML = `<p>Usually, most sample collection is scheduled between 5 AM and 2 PM. However, your availability and convenience remain a priority.</p>`
        } else {
            if (text.innerHTML.length != 0) {
                text.innerHTML=``
            }
        }
    }
}
function p8() {
    for (let i = 1; i < 14; i++){
        let text = document.getElementById(`giveText${i}`)
        if (i == 8) {
            text.innerHTML = `<p>Yes, you can reschedule the appointment. Cancellations, will incur a cancellation charge equal to the pickup fee.</p>`
        } else {
            if (text.innerHTML.length != 0) {
                text.innerHTML=``
            }
        }
    }
}
function p9() {
    for (let i = 1; i < 14; i++){
        let text = document.getElementById(`giveText${i}`)
        if (i == 9) {
            text.innerHTML = `<p>You can pay through Netbanking, Credit Card, Debit Card or E-wallet (PayU).</p>`
        } else {
            if (text.innerHTML.length != 0) {
                text.innerHTML=``
            }
        }
    }
}
function p10() {
    for (let i = 1; i < 14; i++){
        let text = document.getElementById(`giveText${i}`)
        if (i == 10) {
            text.innerHTML = `<p>No. Since the Lab tests are pre-booked, the payment should be made online, on Lybrate app or website.</p>`
        } else {
            if (text.innerHTML.length != 0) {
                text.innerHTML=``
            }
        }
    }
}
function p11() {
    for (let i = 1; i < 14; i++){
        let text = document.getElementById(`giveText${i}`)
        if (i == 11) {
            text.innerHTML = `<p>Yes, the test reports are also delivered to your registered email Id.</p>`
        } else {
            if (text.innerHTML.length != 0) {
                text.innerHTML=``
            }
        }
    }
}
function p12() {
    for (let i = 1; i < 14; i++){
        let text = document.getElementById(`giveText${i}`)
        if (i == 12) {
            text.innerHTML = `<p>Depending upon the test(s) you book, the relevant testing and report generation time will be pre-communicated to you. As soon as the report is generated at the lab, it will be made available to you. No more waiting for a couriered report.
</p>`
        } else {
            if (text.innerHTML.length != 0) {
                text.innerHTML=``
            }
        }
    }
}
function p13() {
    for (let i = 1; i < 14; i++){
        let text = document.getElementById(`giveText${i}`)
        if (i == 13) {
            text.innerHTML = `<p>Test Report will be shared via email and can be viewed from 'Lab Reports' section on 'My Account' on your Lybrate account.</p>`
        } else {
            if (text.innerHTML.length != 0) {
                text.innerHTML=``
            }
        }
    }
}


